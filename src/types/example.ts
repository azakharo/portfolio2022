export interface ExampleData {
  id: string;
  nameKey: string;
  imgPath: string;
  urls: string[];
  descKey: string;
  tags: string[];
  year: number;
  customer?: string;
  customerKey?: string;
}
